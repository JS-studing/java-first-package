package inc.nucleargames.butlers.interfaces;

/**
 * Interface of person, who greets every guest.
 */
public interface IButler {
    /**
     * Greet every guest.
     * @return String with greeting.
     */
    String greet();
}
